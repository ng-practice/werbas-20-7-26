form(*ngIf='book', #form='ngForm', (ngSubmit)='save(form.value)')
  .form-group-custom
    label Title
    input.form-control(
      type='text',
      [(ngModel)]='book.title',
      name='title',
      #title='ngModel',
      required
    )
    .form-error([hidden]='!title.errors?.required || title.pristine')
      | Please specify a title.

  .form-group-custom
    label Subtitle
    input.form-control(
      type='text',
      [(ngModel)]='book.subtitle',
      name='subtitle',
      #subtitle='ngModel',
      required
    )
    .form-error([hidden]='!subtitle.errors?.required || subtitle.pristine')
      | Please specify a subtitle.

  .form-group-custom
    label Authors
    input.form-control(
      type='text',
      [(ngModel)]='book.author',
      name='author',
      #author='ngModel',
      minlength='2'
    )
    .form-error([hidden]='!author.errors?.minlength || author.pristine')
      | Please insert two characters, at least.

  button.btn.btn-primary(type='submit') Save
